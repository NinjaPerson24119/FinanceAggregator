STANDARD_COLUMNS = {
    "DATE": "date",
    "NAME": "name",
    "AMOUNT": "amount",
    # this column is not in the CSV, but is added by FinanceData based on the configuration
    "SOURCE": "source",
}
